import 'package:flutter/material.dart';

class MiddleSchoolUnitCoordinatorEditMentorMentee extends StatelessWidget {
  const MiddleSchoolUnitCoordinatorEditMentorMentee({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Mentor/Students')),
      body: const Center(child: Text('Edit Mentor/Students Page')),
    );
  }
} 