import 'package:flutter/material.dart';

class UniversityUnitCoordinatorStudentStats extends StatelessWidget {
  const UniversityUnitCoordinatorStudentStats({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Student Stats')),
      body: const Center(child: Text('Student Stats Page')),
    );
  }
} 